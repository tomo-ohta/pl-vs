# V05 オブジェクトの輪郭・PBR改善（2026-09-17）

## 状態

**一次改善を実装・検証済み。全オブジェクトのプロ品質到達を意味しない。** 既存のモデル置換機構を復旧し、専用形状が必要な車・植栽を追加した。V05の接写品質、全家具の意味タグ、LOD、最終の目視承認は継続対象。

## 実装した内容

| 対象 | 変更 | 適用範囲 |
|---|---|---|
| 車両 | 車輪の切り欠きを持つ押し出し車体、傾斜ガラス、A/Bピラー、屋根、タイヤ、リムとスポーク、ミラー、取っ手、ドア境界、グリル、灯具、簡易車内。1台16,388三角形 | StreetGeneratorのcar、U16のcarAt。既存車のみ。空のC06へ新規車両は追加しない |
| 塗装・ガラス | 既存carPaintのclearcoat、carGlassの物理材質、金属・ゴム・内装材を分離。部屋の照明・霧・影の共有経路を使用 | 車両の材質別メッシュをチャンクで結合 |
| オフィス机 | Poly Haven metal_office_desk、1Kカラー・normal・ARMのglTF | kind=desk。U14 seed42では88台、E09/R16等も寸法適合時に置換 |
| テーブル | Poly Haven dining_table、1K PBR | kind=table。E17 seed42で16台。テーブルクロス付きのモデルなので全家具への流用は禁止 |
| 鉢植え | pachira_aquatica_01のa個体、幹・葉のPBR。既存の鉢を回転体の陶器形状にし、土を独立材質で追加 | plantUnitの鉢と葉をpropGroupで関連付け、葉だけをモデルへ置換。鉢の衝突を維持。U06/U14で各2株 |
| その他の植栽 | 球体の葉群を湾曲した葉・葉柄・枝へ、plantの反復箱も葉群へ、grassの反復箱は曲がった草葉へ置換 | 共通RoomBuilder。lowでは密度低減。薄いソリッドの生垣仕切りは境界を見失わないよう既存表示を維持 |
| モデル描画 | 通常起動で有効、初室生成前にindex読込待ち。モデル内の同材質メッシュを結合してInstancedMesh化、投影・受影。配置角ジッターを廃止し枠からの突出を防止 | `?props=0`でglTF置換のみ無効。車・手続き植栽の無効化ではない |
| 非同期読込 | 代替箱を材質×チャンクで管理。モデル到着時に差替え、破棄済み部屋には追加しない | 既存の寿命管理に統合 |

候補10モデルを取得・検討後、今回実際に採用した3モデルのみを配布。不要な7モデルは今回取得分から除去した。既存manifestの69件は過去の取得記録であり、現行配布数ではない。実体は`public/cc0/models/index.json`を参照。

### 由来と再現

- [Poly Haven CC0利用条件](https://polyhaven.com/license)
- [metal_office_desk](https://polyhaven.com/a/metal_office_desk)、[pachira_aquatica_01](https://polyhaven.com/a/pachira_aquatica_01)、[dining_table](https://polyhaven.com/a/dining_table)
- 取得定義: `assets/cc0/selection-props.json`
- 再取得: `python3 tools/fetch-cc0-assets.py assets/cc0/selection-props.json --models`
- 配布書出し: `npm run build:cc0:models`
- 車、枝葉、草、鉢は本プロジェクトでコード生成。新しいAI画像生成は行っていない。従来の生成テクスチャと採用モデル付属PBR画像を使用。

## 不変条件

車・鉢に表示用メタデータを追加するだけで、元のmin/max/solid、配置数、乱数呼出し、入口・出口、セーブ形式は変更しない。RoomBuilderは元のboxから衝突を作成し、置換は描画専用。車は表示全体のAABBを既存の車体幅・長さ・元部品全体の高さに収める。葉・草も元の表示枠を超えないことをテストする。glTFは一様縮尺と既存の75万三角形/室予算を適用。植栽モデルのみ縮尺下限を0.65とし、上限は1.25。広い植栽帯へ小さな鉢を配置しない。

衝突と形状の厳密一致は未達。机列の隙間や葉群の透けた場所にも元の衝突体がある。ゲーム仕様を変えずに次段階で接写と移動を確認すること。

## 検証

- `npm run typecheck`、`npm run build`成功。既存の大きなバンドル警告は残る。
- `node tests/object-geometry.mjs`: X/Z方向車体の境界・有限座標・入力非破壊、5寸法の枝葉の決定性・境界・low密度、草の境界・三角形上限に合格。
- 開発用素材レビューの「オブジェクト検査」でU06/U14/U16/R07/E17 × low/mid/highを描画。shader、texture、propの読込エラー0。結果は`validation.json`。
- 同一seedの品質切替で各室の衝突数が一致。全seed・全variantの移動試験、実機スマホ、GPU時間の測定は未実施。
- レビュー画像は撮像効果OFF、seed42、variant0、high、露出1.25。素材レビューの照明・トーンマッピングであり、本編の最終撮像品質判定とは区別する。

### 保存画像

| 車 | 鉢植え |
|---|---|
| ![U16](U16-car.png) | ![U06](U06-plant.png) |

| オフィス机 | テーブル |
|---|---|
| ![U14](U14-desk.png) | ![E17](E17-table.png) |

温室の共通植栽は`R07-foliage.png`。各画像のJSONにカメラ・描画条件・モデル数を保存。

## 次のAIが行う作業（優先順）

1. **車の接写品質**: 現在は識別性を改善した手続き型セダン。ボンネット・フェンダー・ルーフの断面を連続曲面へ、ワイパー・灯具内部・タイヤ溝・ドアの実際の開口線を追加。車用のUVと局所汚れを制作。実物写真に対して側面・前後斜め・低視点で比較し、平板に見える面をなくす。採用可能な通常車種の高品質モデルを見つけた場合もライセンスと寸法を記録する。
2. **植物の種別化**: 葉群・草は形状近似であり、植物学的に正確な樹種モデルではない。生垣、シダ、蔓、樹木を分け、正しい葉・茎の接続と葉脈、裏面の明るさを実装。R07の吊り植物は支持点との接続も確認。薄い衝突付き植栽仕切りを疎な葉へ置き換えて透明な障害物にしない。
3. **残る家具**: U10/C09等の机・椅子は部品boxの集合でkindがない。生成器の意味を使ってpropGroupと方向を付与し、既存部品を重ねて残さない。椅子、ソファ、棚、ロッカー、CRTなどの輪郭を順に改善。材質だけで全家具を机に分類しない。現在の88台のオフィス机や16台のクロス付きテーブルを別種家具へ流用しない。
4. **照明と接地**: モデルの静的照明はインスタンス中心の定数サンプル。脚、天板裏、葉裏の細かな陰影はまだ不足する。モデル頂点AO/ライトプローブかモデル用焼き込みを実装し、GTAO・影と重ねて黒くなりすぎないよう検証する。
5. **性能**: メッシュの結合はdraw call削減のみ。lowでもglTFは同じ密度のまま。車・家具・植物のLODを作成し、距離と画面占有率で切替。R07の枝葉密度、U14の机列は実機GPU時間とストリーミング負荷を測って決める。検査の三角形数にはガラス透過の追加パスが含まれ得るのでbuilt.trianglesと混同しない。
6. **最終受入**: 本編で移動・扉・隣室・セーブ復元を確認。車・机の見た目と元衝突のずれを記録。今回の15描画ケース合格をV05全体や「プロ品質」の合格と扱わない。

## 第 2 段（2026-09-21）: 残る家具の意味タグと専用形状

「次のAIが行う作業」3 番。生成器の家具ヘルパに表示用タグ（`Box.propGroup` = 1 台分のグループ、`Box.kind` = 種類）を付け、
`ArchitecturalDetails.detailedBoxes` → `src/render/props/FurnitureShapes.ts` がグループ単位で「箔の集合」を家具の形に描き替える。
RoomBuilder が受け取れるのは箱だけなので、全て軸平行な箱の組（傾いた背・円柱の脚は `docs/visual-requests.md`「B → RoomBuilder」のフック待ち）。
当たり判定・配置・乱数・セーブ形式は不変（タグは描画専用。`tests/furniture-shapes.mjs` で入力非破壊・決定論・外接枡・三角形を検査）。

### タグを付けた場所

| 生成器 | 家具 | propGroup / kind | 描き替え |
|---|---|---|---|
| `furniture.chair` | 椅子（C09 食堂・C14 休憩室・E/M のドレッシング） | `chair@x,z` / 座 `chair` | あり |
| `furniture.linkedSeats` | 連結椅子（C10 / U06 待合） | `linkedSeats@x,z` / 梁 `linkedSeats` | あり |
| `CorridorGenerator.bench` | 病院廊下の 3 人掛け（C05 / R17） | `linkedSeats@dir,t,face` / 座板 `linkedSeats` | あり（同じ形状） |
| `furniture.bench` | 木のベンチ（C11 / U13 更衣室） | `bench@x,z` / 天板 `bench` | あり |
| `furniture.lockerBank` | ロッカー列（C11 / U13。U13 は lockerBlue に再着色されてもそのまま効く） | `lockers@dir,a0,face` / 本体 `lockers` | あり |
| `furniture.vending` | 食堂の自販機（前面の箔が既に方向付き） | `vending@…` / 本体 `vending` | タグのみ |
| `CorridorGenerator.decorDoor` | 廊下の装飾扉 | `decorDoor@…` / 扉箔 `decorDoor` | タグのみ（将来の扉モデルの目印） |
| （既存 kind）`common` の 1 箱椅子 `chair`、SmallRoom / U04 の 1 箱自販機 `vending` | — | — | 椅子は最寄りの机（無ければ部屋の中心）向きに描き替え。自販機は部屋中心側の面に商品窓・取り出し口（前面に発光箔があれば窓は省く） |
| （タグ無し・推定）机上の `screenDark` / `screenLcd` 箔（E09 LCD、M の CRT） | — | — | 机の天板上・幅 0.25〜0.8 の薄箔だけ。机の中心線の外側を正面とし枡（ベゼル）を付ける。背に furniture* の筐体箔が接していれば段付きの CRT 筐体に置き換え、無ければ薄い背面カバー |

### 形状と三角形（箱 1 個 = 12、面取り材質で最小辺 8 cm 超 = 108）

| 家具 | 形状 | 箱 / 三角形（1 台） | 変更前 |
|---|---|---|---|
| 椅子 | 3.5 cm の座面 + 側の座枠 2 本 + 鋼管脚 4 本（後脚は背の支柱まで通す）+ 支柱の前面に付く背板（高 22 cm） | 8 / 96 | 6 / 72（座と背の厚板） |
| 連結椅子 4 席 | 座直下の梁 + 両端の板脚 + 席ごとの座（隙間 3 cm）+ 背パッド（高 30 cm）+ 背の支柱 | 15 / 180 | 11 / 132 |
| 廊下ベンチ 3 席 | 同上（座板・背板・脚板の外接枡に収める） | 12 / 144 | 3 / 36 |
| 木のベンチ 1.8 m | 板 3 枚（隙間 2 cm）+ 鋼管支柱 2 × 3 + 長手の貫 | 10 / 120 | 4 / 48 |
| ロッカー 1 扉 | 前面を 1.2 cm 引いた本体 + 目地の底（暗い前板）+ 台輪 + 天端（列で 4 箱）、扉ごとに独立パネル（目地 1.4 cm が実際の凹み）+ 通気口 2 × 2 + 取っ手（座板 + つまみ） | 7 / 84（+ 列 4 箱） | 6 / 72 |
| LCD モニター | 画面箔 + 枡 4 辺 + 背面カバー | +5 / +60 | 画面箔のみ |
| CRT | 枡 4 辺 + 段付き筐体 3 段（前枡 7 cm → 胴 → 後部） | +7 / 約 280 | 筐体 1 箱 |
| 売場の棚（shelfMetal、高 2 m 未満） | 支柱 + 段板 3 cm（上面は従来の level + .05 のまま。商品箔・段ボールが接する）+ 両長辺の縁 4.5 cm + 両端の側板 + 中央の背板 | 段あたり +2 箱、列あたり +3 箱 | 5 cm の一様な段板 |
| 収納（furniture* の cabinet 相当） | 扉の目地と取っ手を「部屋の中心を向く面」に付ける（従来は常に −z 面で、壁沿いの棚では壁側に付いていた） | 同数 | — |

三角形の削減判断: 椅子の前後の座枠（C09 で 1.33 倍）、連結椅子の受け金具と足元板（C10 で 1.41 倍）、ロッカーの通気口 3 本目（C11 で 1.44 倍）は
部屋合計の上限 1.3 倍を超えたので省いた。

### 部屋別 `built.triangles`（seed 7、`?nolock=1&new=1&seed=7&force=<ID>`、high）

変更前は「変更後の値 − 表示箱の三角形見積りの差」で推定（同じ seed のレイアウトは同一）。

| 部屋 | 対象 | 変更前（推定） | 変更後 | 比 |
|---|---|---|---|---|
| C09 社員食堂 | 椅子 435、自販機 3 | 62,490 | 72,930 | 1.17 |
| C10 市役所待合室 | 連結椅子 120 列（480 席） | 35,310 | 41,070 | 1.16 |
| C11 体育館更衣室 | ロッカー 12 列 418 扉、ベンチ 35 | 48,520 | 58,456 | 1.20 |
| C05 病院外来廊下 | 3 人掛け 3、装飾扉 6 | 4,950 | 4,962 | 1.00 |
| E09 垂直水面オフィス | モニター 32 | 474,290 | 476,210 | 1.00 |
| U15 単一商品スーパー | 棚 77 列 | 154,776 | 176,072 | 1.14 |
| U04 無名自販機室 | 自販機 4 | 5,366 | 5,286 | 0.99 |

いずれもコンソールエラー 0。`npx tsc --noEmit -p .` 0 エラー。`node --import ./tests/ts-resolve.mjs tests/furniture-shapes.mjs` 合格
（`tests/ts-resolve.mjs` は拡張子なし import を Node で解決する登録フック。既存の `tests/object-geometry.mjs` は従来どおり直接実行できる）。

### 接写の確認（Browser pane。画像は保存していない）

- C09: 列端の椅子を側方 1.4 m から。細い座面・背板と支柱・4 本脚が読め、背は机と反対側にある。遠景の 400 脚も椅子の列に見える。
- C10 / C05: 連結椅子を前方斜めから。席ごとの座と背パッド、梁、両端の板脚。廊下の 3 人掛けは壁付きで背が壁側。
- C11: ロッカー列を正面から。扉ごとの目地が暗く落ち、通気口 2 × 2、取っ手が並ぶ。ベンチは 3 枚板と鋼管脚。
- E09: 机上の LCD にベゼルと背面カバー。宙に浮く黒板には見えない。
- U15: 売場の棚の端。側板・中央の背板・段板の縁で「什器」に読める。商品（furnitureLight の箔）は従来どおり箱。

### 残課題（第 3 段以降）

- **傾き・円柱**: RoomBuilder のジオメトリフック（visual-requests「B → RoomBuilder」）が入ったら、椅子の背 8° 傾斜・鋼管の 8 角柱・CRT の膨らみを `src/render/props/FurnitureGeometry.ts` に実装する。
- **ソファ**: L04（legendary.ts、seatRed の座 + 背の箔）、`furnishGeneric` の `sofa` kind の島は未着手。座の分割・アーム・台輪を同じ仕組みで描ける（legendary.ts は A2 の所有なのでタグは付けていない。材質 seatRed + 隣接する背の箔からの推定で対応可能）。
- **U11 / L08 の座席列**（furnitureDark → seatBlue の再着色、背が無い）、**Theater の座席**（統合担当が編集済み）、**U14 の CRT**（InstanceSpec。箔ではないので対象外）。
- **R08 の書架**は rare.ts が既に段板 + 背板 + 支柱に置き換えている。U10 / U14 の bookshelfWood の棚（uncommon.ts）は段板付きなので今回は触っていない。
- 変更前の三角形数は見積り。厳密な前後比較は `git stash` 相当の切り替えが要る（並列作業中は不可）。
