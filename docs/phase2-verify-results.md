# Phase 2 ブラウザ検証結果

## ブラウザ検証 1/4（C11 U04 U08 U12 U16 U20 R04 R08 R13 R17 E01 E06 E11 E15 E19 L03 L08 L12 L16 M01 M07 M13 M18）

手順: `http://localhost:5173/?nolock=1&new=1&seed=<S>&force=<ID>` → 開始クリック → `window.game` で対象ノードを探し `spawnPointOf` → `teleport` → `enterRoom` → `step(1/60)` × 120。入室前後の `layoutFor(n).boxes.length` / `sockets.length` 不変、`player.pos` が `worldBounds` 内、`world.log` の ERROR 0 件を全室で確認。Tier: high。

共通の注意:
- コンソールの error は全室で同じ 7 件（`this.levelSpanFor is not a function` / `installWorldHooks is not defined` / `themeDoorMatAt is not defined` / `SNAPSHOT_EXCLUDE_LAYER is not defined` / 500 ×3）。いずれも `main.ts?t=17895370xx…` の **本セッション開始前の HMR リロード時のもの**（他担当の編集途中）で、私が開いたページの読み込み・操作では新規 error は 1 件も増えなかった（毎回 `onlyErrors` で件数 7 のまま）。
- Browser ペインが非表示のとき rAF ループが止まる（`game.audio.now` だけ進む）。時間依存の検証（DynamicMapNode の swap、MovingWalls の位相、ride の 5 秒）は `game.step(1/60)` の連続呼び出し（シミュレーション時間）で行った。AudioEvent は実時間（AudioContext）で動くので実待機で確認。
- テレポート先が家具・ブロックの内部だと 1 フレームで天井の上（y = 天井高）へ押し出される（R08 本棚、E19 迷路壁、M07 ブロックの中心で発生）。ゲーム操作では起きない harness 側の事象なので fail にはしていない。

| ID | seed | 部屋 | status | 確認した事実 | 備考 |
|---|---|---|---|---|---|
| C11 | 11 | r2 更衣室 | ok | `zones.filter(friction).length===1`, `friction===0.6`, ゾーンが部屋全体、`zonesOf(id).length===1`。隣室 5 部屋は friction 0。6 フレーム前進距離 0.0951 m（隣室 C02 は 0.1971 m、比 0.48） | 見た目変化なし（仕様どおり） |
| U04 | 4 | r2 無名自販機室 | ok | 自販機（furnitureDark, 高 >1.7）3 台、`signPlate` 60 箔、`signs` 6 枚（fs:price/brand ×3）全て `text===''`、built.signs 6 | スクショで白無地ラベルの自販機 3 台を確認 |
| U08 | 8 | r2 ホテル廊下 | ok | `render.wetness===0.8`、水たまり decals 3（water/normal y）、`wallDark` 帯あり、audio `floor=wet`（preset 湿った足音・水滴）。friction 0.5 ゾーン全体、前進距離比 0.353（隣室 carpet） | 廊下が細く水たまり 3 個（既知の制約どおり） |
| U12 | 12 | r3 | ok | `particles.type==='steam'`, aabb 底面積 29.45 (<60)、粒数 32（20〜60）、Points 'particles/steam' count 32、`effects===1`、ShaderMaterial の `uTime` が step で進む | 湯気は alpha 0.12 でスクショでは判別しづらい |
| U16 | 16 | r3 番号異常駐車場 | ok | `dn:*` サイン 48 枚（上限）、全て bg 0xd9b52e・`/^B\d-\d\d$/`、欠番あり（02, 20）、重複あり（07/10/14/16/17 が別柱） | 柱の黄帯上のプレートを確認 |
| U20 | 20 | r2 屋内モーテル中庭 | warn | `skyOvercast` 箔あり・天井箔なし、`lights===1`, `directional===1`, `skyAmbient` あり、`sky.preset==='overcast'`, `ambient===0x9aa2ac`、`effects===1`、空メッシュは部屋専用材質（disposable / 独自 map）、120 フレームで map.offset が (0.0016, −0.0037) 流れる | **見た目**: `skyOvercast` は MaterialLibrary で `texture:'diffuser'`（12 m リピート）のため、天井を見上げると曇天ではなく拡散板の格子模様が明るく光って見える（skyDusk/skyNoon も同じ定義）。雲テクスチャが無い限り仕様の「曇天」には見えない |
| R04 | 44 | r3 屋内住宅街 | warn | StreetGenerator 本実装: `chunkSize 32`, `floorAsphalt`, sockets `entry:door, street0/1:street, exit0:door`、窓 261、建物 7、`lights===1`（FakeSky）、signs 8（BAR/24H/CLINIC/ランドリー）、`path 3`、`skyDusk` 箔、`preset 'dusk'`, `ambient 0x7a5c50`、日射仰角 18.0°、height 9 | footprint 64 × 53.5 m（doc の Rare 上限 48 より大きい。growToFill か）。夕空も diffuser 格子（U20 と同じ） |
| R08 | 88 | r2 無書籍図書館 | ok | `rides===undefined && zones===undefined`、実時間 55 s で `audio.events` に pageTurn 4 件（壁際 0.6 m・高さ 1.2）、preset 遠いページ音 | 音イベントは実時間ベース（sim step では増えない） |
| R13 | 133 | r3 巨大遊具広場 | ok | `height===15`、footprint 78 × 94.5 m、全 socket door 1×2.1、`holes 0`、`lights[0].intensity===9`（palette 1 の 9 倍）、`chunks 12`、遊具箱最大高 3.29 m | 巨大空間に通常扉。スクショ良好 |
| R17 | 177 | r2 無限病棟 | warn | `signs` に `rd:num`（plate）、build で atlas セルが「病室 401〜420」（base 4。`spec.text` の 701 は layout 時の仮値で正常）、end → r7 が `R17/role repeat/repeat 1`、state `{index:1, base:4, headRoomId:r2}`、r7 のサイン「病室 421〜440」、r7 の end → r25 が repeat 2 | **見た目**: variant 1 で入口脇サイン（local x −1.588, z 1.5, 幅 1.2）が側壁の装飾扉（偽扉）と同じ位置に置かれ、扉枠に隠れてほぼ読めない（扉の両脇に黒い縁だけ見える） |
| E01 | 101 | r2 閉ループ廊下 | ok | `end` が seam+far、`targetRoomId==='r1'`（祖先不足で開始部屋）= `modifierState.LoopTopology.targetRoomId`、`INFO far link r2/end -> r1`、扉前で E → transition → `currentRoomId==='r1'`、r1 入口内側 (0,0,1.5) yaw π（前向き）に着地 | side0 は通常接続 |
| E06 | 106 | r3 逆影広間 | ok | `lights.length===1`, `lighting.occlusion===false`, `shadowDecal` 41 枚、発光パネル 1 枚、lightOff 64、`InvertedShadow/playerBlob.visible===true`、光源直下で scale x 1.82→0.80、柱のデカール中心が光源側（dot>0） | スクショ: 消灯グリッド + 中央 1 灯 + 足元ブロブ |
| E11 | 111 | r2 線路のないホーム | ok | `rides[0]` = train/20 s/path 1、ride ゾーン 1、乗車口 exit1: `ride && seam && !locked && !open`、`modifierState.VehicleRide.socketId==='exit1'`、`carPaint` 16、扉前 HUD「E: 乗る」、E → `state==='riding'`、`VehicleRide/streaks.visible===true`・流れる、車内スクショ良好（HUD「E: 到着を早める」）、5 s 後 E → r28 = `adapter.kind==='platform'`、`P.far===true`、log `ride start` / `ride r2/exit1 -> r28` | platform 側の車両は AdapterGenerator 担当 |
| E15 | 115 | r2 小数階フロア | ok | signs: `fs:floor:entry=6.1F`, `up0/up1=6.11F`, `elev=6.111`, `stair=↑ 6.11F`、built.signs 5 | 階段サインは ↑ のみ（↓ は上階側か） |
| E19 | 119 | r3 温度座標迷宮 | ok | 灯色 19 種（寒色〜暖色）、`lightWarm` と `lightPanel` 両方あり、HUD ヒント: 入口 30.3℃ → 中央 23.2℃ → exit0 前 8.5℃（`/^\d+\.\d℃$/`、hotExit=false の逆勾配）、扉を見ている間は「E: 扉を開ける」 | `import('/src/…/TemperatureField.ts')` は別モジュール実体になり `temperatureFieldOf(L)` が null（HMR の ?t= 付き URL を使えば取れる） |
| L03 | 303 | r2 倉庫内麦畑 | ok | MegaHall 本実装: `chunkSize 32`, `shellCount 192`, theme zones 4（南西/南東/北西/北東の畑）、`instances` = ['grass','boxCardboard'] (9000 / 4503)、bounds 185.5 × 115.5、height 14、`fogFar 90`、`chunks 24`、`path 2`、出口 5 本 幅 2.2、木箱 4 | 麦は sway なし静止（既知） |
| L08 | 308 | r2 | ok | depth 1 に直接配置（LargeRoom 代替 33 × 64）、`portals` に far+seam（exit4 → r1）、`modifierState.LoopTopology.climbed===1`、E で開けて r1 入口へ着地、`INFO far link r2/exit4 -> r1` | — |
| L12 | 312 | r2 設備大聖堂 | ok | `INFO legendary far placement r1/entry -> r2 (L12)`（B1F, y −3.6）。theme zones = 配管橋/身廊/西側廊/東側廊、配管橋スラブ（metal, y 3.6, 幅 >10）あり、階段 2、`sockets.every(!crawl)`、内装ソリッド 165 個 最大高 15.55 m（ScaleAnomaly perProp）、height 16.5、55.5 × 157 m、`chunks 12` | 中央は意図的に暗い（高所灯半分消灯） |
| L16 | 316 | r2 空間博物館 | ok | `modifierState.GraphReference` = {mode 'past', count 8, exhibits[C02, — ×7]}、`gr:*` サイン 8、built.signs 8、`GraphReference/photo*` 8、名札 atlas「オフィス廊下」+ 空額縁「—」、写真は C02 の細い足跡シルエット | 訪問 2 部屋なので 7 枚が空額縁（仕様） |
| M01 | 501 | r2 地図室 | ok | `effects===1`、`SelfMap/panel` あり（map 1024 × 580, high）、入口対面の壁（z −11.83）、`sm:title:3` = 現在地図、机 + 灯。隣室 U11 へ行って戻ると板のキャンバス（64×36 縮小の明画素 58 → 219）が更新 | 地図の更新は step 駆動 |
| M07 | 507 | r2 動くマップタイル | ok | DynamicGrid 本実装: footprint 30 × 27、height 3.4（variant 0）、floorTile/wallWhite、socket が 3 m 格子上、exits 4、`dynamics` 7（`MovingWalls:grid0..6`、amp 3/6、period 12/24、幅 1.7、高 3.35）、掃引が回廊/扉前に入らない、固定ブロック 15 の隙間 ≥1.2、`MovingWalls/…grid*` コライダ 7 と `dynamic/…grid*` 非表示・非ソリッド、zone theme:grid、PointLight が掃引域に無い、bay サインあり、黄線・レールあり。停止規則: grid0 の進路 1.3 m 手前に立つと 60 フレーム aabb 不変、離れると再開。DynamicMapNode: 前進扉 4 枚全て seam、電子サインが行き先名、床を見て 1500 step で `swapCount 1`（exit0↔exit3）、`localFlags['seam:*'].forceDefinitionId` と pending 一致、サイン文字交換、交換後 exit0 を開けると `definitionId==='C02'` = pending | 実時間 24 s の待機では swap しない（rAF 停止のため。sim step で確認） |
| M13 | 513 | r3 | ok | `render.style==='untextured'`, `boxes.length===shellCount (19)`, `lights 0`, `labels 0`, `ambient 0xffffff`, fog/背景 0xe6e6e2、group 内 21 Mesh 全て material.name `'untextured'`、doorGroup は doorWood/metal/doorMetal | 材質名は `untextured`（doc の `untextured|…` ではない） |
| M18 | 518 | r28 天候記憶室 | ok | `?force=R06` で R06 に入室後、`RarityGenerator.setForcedRoomId('M18')`（app と同じ `?t=` URL を import）で次の抽選を固定 → M18 が r8 の子として作成。`AmbientCarryover` = {sourceRoomId r2 (R06), reason 'rare', audioPreset 低周波・水音, fog {near 6, far 35}}。入室後 palette.fog/ambient/lightColor が R06 と一致、`built.fog` = R06 の `render.fog`、scene.fog 1a4a8a/6/35、PointLight 色 9fc8ff、audio preset が「低周波・水音」に切替 | 焼き込み色（SurfaceLighting）は既知の制約どおり |

集計: ok 20 / warn 3（U20, R04: 空材質が diffuser 格子で曇天・夕空に見えない / R17: 入口脇サインが偽扉と重なる）/ fail 0 / notPlaced 0。全 23 部屋で例外・ERROR ログ・不変条件違反なし。

## ブラウザ検証 2/4（U01 U05 U09 U13 U17 R01 R05 R09 R14 R18 E02 E07 E12 E16 E20 L04 L09 L13 L17 M02 M08 M14 M19）

手順: 1/4 と同じ（`?nolock=1&new=1&seed=<S>&force=<ID>` → 開始 → `spawnPointOf` → `teleport` → `enterRoom` → `step(1/60)` × 120。入室前後の `layoutFor(n).boxes.length` 不変、`player.pos` が `worldBounds` 内、`world.log` の ERROR 0 件、コンソール error が基準の 7 件（1/4 と同じ HMR 時の残骸）から増えない、を全室で確認）。Tier: high。

共通の注意:
- 時間依存の演出は 2 系統ある: `dt` 積算のもの（MapRotation の drift、ShallowWater/ExternalForce の物理、LightingPhase の明滅）は `game.step(1/60)` の連続で進む。**実時間（performance.now）基準のもの**（PastWindow E12 の 10 s 間隔・R09 の 3 s 遅延、Game.transition の 280 ms フェード、MapRotation の退室時 0.6 s 戻し）は sim step では進まず、`await setTimeout` の実待機で確認した。
- Seam 扉の E → 遷移は `state==='transition'` のまま止まるので、0.9 s 実待機してから `currentRoomId` を読む。
- 家具の中へテレポートすると 1 フレームで天井上へ押し出される（R09 の座席列 y 1.5、1/4 と同じ harness 側の事象）。

| ID | seed | 部屋 | status | 確認した事実 | 備考 |
|---|---|---|---|---|---|
| U01 | 1 | r3 蛍光灯位相廊下 | ok | L 字廊下、light 材質 {lightYellow, lightGreen}（2 色。3 色揃わないのは seed 依存の想定内）、`lights` 4 本全て許容色（f6e6a0 / c8f0c0）、`effects===1`、発光メッシュ 2 枚とも `userData.disposable` に材質あり、emissiveIntensity が 120 → 300 フレームで 2.37/2.30 → 2.64/2.75 と揺れる | スクショ: 手前が黄、奥が緑の区画 |
| U05 | 5 | r2 出口のない EXIT | ok | Z 字廊下、`fs:exit`（emissive 'EXIT'）+ `fs:exit:ahead`（'↑ EXIT'）、EXIT 直下にソケット無し、`end` は側壁（dir 1、entry dir 2 と軸が異なる）、`ex.dir !== (end.dir+2)%4`、緑灯 0x4cff80 あり、シェル材質整合。偽扉の前で HUD ヒント空（インタラクト不可）、`end` の前は「この扉は開かなそうだ」、`side1` は「E: 扉を開ける」 | この seed では真の `end` と `side2` が施錠（`locked`、`targetRoomId` null、log に dead-end WARN 無し）で進行は side1 のみ。U05 の責任ではなく WorldManager の接続結果 |
| U09 | 9 → 19 | r2 / r3 鏡のずれる洗面所 | ok | seed 9（12×12、扉 5 枚）は `mirrors.length===0`（既知の制約: 鏡面近くに扉）。seed 19: `mirrors.length===1`（pos [5.5,1.3,6] dir 3、size 11.7×1.3、offset 0.3、delay 0.5）、`glass` ソリッド 1、疑似扉パネルあり、`effects===1`、`MirrorOffset/proxy` visible、前進 30 フレーム後 proxy はほぼ不動（0.03 m）→ 10 フレーム後 0.4 m → 遅れて追従、しゃがみで scale.y 0.5、boxes 63 不変 | ガラス越しに反転コピーの木製ブース列が見える |
| U13 | 13 | r2 プールのない更衣室 | ok | `render.wetness===0.4`、`render.fog` {bfe6e6, 8, 40} = `built.fog` = scene.fog、water decals 16（normal y）、`wallDark` 帯あり、audio preset 換気・水滴・遠い反響 / `floor=wet` | 床の水たまりと薄い青緑の霧を確認 |
| U17 | 17 → 117 | r3 / r2 終了後の展示会場 | warn | seed 17（36×36 LargeRoom、長机 47 台）: ブース 1 つだけ（`fs:tag:0` + `fs:banner:0`、`wallWhite` 2.2 m 背板 1 枚。doc の ≥2 に満たない）。seed 117: ブース 7（tag 7 / banner 7、背板 7）、全サイン `text===''`、built.signs 14 | 既知の制約「furnishGeneric と重なるブースは省く」が seed 17 では極端に効き、展示会場に見えない（長机の広間 + 隅に 1 ブース）。既存家具を退避する等の改善余地 |
| R01 | 21 | r2 浅水タイル回廊 | ok | PoolGenerator 本実装: variant 0 = 十字（footprint 3、`end`/`arm0`/`arm1`）、幅 5、height 4.2、palette floorTile/floorTile/ceilingWhite/doorMetal、shellCount 65、ソケット 0.5 m 格子、デッキ（floorTile 上面 0.32）・柱・色帯 1.05–1.25・レーンライン・排水溝・手すり 2・`pool-notice` サイン・mist 粒・`path` 2 ([0,0,0] 起点)。ShallowWater: water ゾーン 11 = waterShallow 箔 11、slow 0.7、trim ソリッド 12 = 3×4 扉、zonesOf 11、wetness 0.3、水中歩行 2.10 m/s（0.7×3.0）、audio 反響・水音 / floor=wet、内装ソリッド全て footprint 内 | スクショ: 白タイル回廊 + 水路 + 両側デッキ。良好 |
| R05 | 25 | r2 空中連絡橋 | ok | L 字の Bridge（boxes 13: floorConcrete 2 + metal 手すり 11）、`render.fog` {0b0f1a, 5, 40} = built.fog = scene.fog/背景、材質名に `\|fb0f1a:5.0:40`（部屋別 fog variant）、lights 1（bfc7d8）、ambient 8a90a0、audio 風音 | 暗い夜の連絡橋。真っ黒ではない（手すり・遠灯が見える） |
| R09 | 29 | r2 監視映画館 | warn | `effects===1`（keys: material/camera/delaySec/slot/svc/last）、`PastWindow/screen` あり（34.0 × 4.0 m、入口壁 z 0.37）、`material.map.name==='snapshot/0'`、`snapshots.activeCount===1`、void 箔 1、boxes 127 不変。RT(256×144) の画素を `readRenderTargetPixels` で読むと mean 32 / max 56（内容あり） | **見た目**: Theater のスクリーンが入口壁の全幅（34 m）× 4 m で、16:9 の RT が横 2.2 倍に引き伸ばされ、かつ映像が暗い（劇場内が暗いため）ので、近づいても赤茶の帯にしか見えない。座席列が全幅で切れ目なし（側面通路 1.5 m のみ）。スクリーンの位置・比率は Theater Generator 側の問題 |
| R14 | 34 | r2 浸水学校 | ok | water ゾーン 11 = waterShallow 11、slow 0.7、trim ソリッド 15 = 3×5 扉、zonesOf 11、wetness 0.3、holes 0。前庭（入口内側 0.6 m）は `inWaterZone=false` で 3.00 m/s、4 m 内側は `inWaterZone=true` で 2.10 m/s、audio 水音・時計 / floor=wet | スクショ: 水面 + 乾いた前庭 + 縁。良好 |
| R18 | 38 | r3 密閉風洞廊下 | ok | force ゾーン 1 {wind, 2.5}、local vector [1,0,0]（entry → exit0 の主軸）、world vector [-1,0,0]、`player.zoneForce.length()===2.5`。開けた場所で静止 60 フレームのドリフト 2.49 m/s、追い風 5.50 m/s、向かい風 0.50 m/s、ジャンプ中も 40 フレームで 1.68 m 流される、audio 強風 [windStrong] loops 1 | 最初の計測は壁際で drift 0 だった（壁に押し付け）。LargeRoom（長机）上に配置 |
| E02 | 102 | r3 年代階段 | ok | VerticalCore（footprint 空、bounds 8.25×8.3、height 7.2）、theme ゾーン 2（level 0: 1980s / 1: 2000s、下が古い）、壁の 3.5–3.7 跨ぎ 0、`shellCount` undefined、built.zones theme 2、light 色 2 種、audio: 入室直後「年代別環境音 [hvac]」→ 上階 (y 3.6) で「PCファン・電子音」→ 下階で「モニター・蛍光灯」 | 階段を挟んで壁色（cream / white）が切り替わる |
| E07 | 107 | r2 観測依存廊下 | ok | 前進扉 3（end/side0/side1）全て `seam && !locked`、placedNeighbors 1、pending {end:C13, side0:C16, side1:U08} = `localFlags['seam:*'].forceDefinitionId`、`rewire:*` サイン 3（plate、atlas 更新済み）、log `INFO intentional seam` ×3。見る→背を向ける 12 往復で attempts 12–13、`rewires` 3/3/3（上限）、`log` 9 件、差し替え後も flag と pending が一致。`end` の前で「E: 扉を開ける」→ E → 0.9 s 後 `currentRoomId` = r16 で `definitionId===forcedDefinitionId==='C13'` = pending | サイン板の文字は行き先名「湿ったホテル廊下」（U08）を目視。`L.signs[].text` は '－－－'（layout 時の仮値）で `updateSign` 経由で書き換わる |
| E12 | 112 | r2 過去窓回廊 | ok | `PastWindow/screen` 3 枚（[±1.05,1.45,2.55], [1.05,1.45,5.75]）、void 箔 3、`effects===1`（intervalSec 10）。sim 700 フレームでは 1 枚のまま（実時間基準）、実待機 10.5 s で maps [snapshot/0,1,2]・activeCount 3、さらに 10.5 s で最古がローテーション | 額縁の窓に廊下の映像を目視 |
| E16 | 116 | r2 虚偽案内区域 | ok | 直線 27 m、`fs:dir:0:0` '→ 出口' / `fs:dir:0:1` '← 階段'（emissive、両面）、偽扉 2 枚（2.05 m 高、非 solid）、全 5 扉が通常接続 | 直線 1 セグメントなので案内板は中央 1 か所のみ |
| E20 | 120 | r2 色欠損フロア | ok | `render.colorMask` [0,1,1]、lights 全て赤成分 0（e8ff）、隣室 5 部屋の colorMask undefined、材質名に `\|m0.00,1.00,1.00` | スクショ: 室内は青緑一色、HUD は通常色 |
| L04 | 304 | r2 無限グランドホテル | ok | MegaAtrium 本実装: 172.5 × 97.5 m、height 19.8（5 階）、boxes 2851（≤6000）、shellCount 935、theme ゾーン 3（宴会場/ロビー/客室翼）、fogFar 90、chunkSize 28、chunks 28、`elevators.length===1` + elevator Portal、signs 116、`mapCell.levelSpan===5`、up0/up1/up2 が y 7.2、up0 の先 r45 は y 3.6 に配置（`legendary far placement r2/up0 -> r45`）、階段最上段 3.6k（k=1..4）あり | depth 1 直接配置、部屋は y −3.6（B1F 表示）。ロビー・上階回廊を目視 |
| L09 | 309 | r2 無限温浴施設 | ok | MegaAtrium 'bath': 146 × 107 m、height 9.6、theme ゾーン 20（大浴場/浴場/更衣室/プール/休憩所）、water ゾーン 12（slow 0.55 / 0.6）、waterShallow 12、chunks 24、`render.wetness===0.7`、mist {aabb y 0–0.6, size 2.2}、Points 120 粒（cap 1000）、water decals 40 + wallDark 帯、`effects===1`、audio 水音・換気 / floor=wet、`hole` ソケット 1 + up0 | スクショ: 柱列と腰壁で区切られた浴場。良好 |
| L13 | 313 | r2 サーバー森林 | warn | ServerGrid 44 × 44、`chunkSize 24`、chunks 4、`instances` = [metal 1824, ledBlue 10032, screenGlow 608] 合計 12464、InstancedMesh 12 個 count 合計 12464、入室前後で不変 | doc の「合計 3000〜8000、12000 超で間隔を広げる」に対し 12464 で上限超え。`L.boxes` に非ソリッド `ledBlue` 95 個が残る（「元の ledBlue 詰め物は消える」に不一致。見た目はラックの LED 帯として自然） |
| L17 | 317 | r2 無限団地 | ok | StreetGrid 本実装: 96 × 96、`height 18`（12 × 1.5）、floorAsphalt、chunkSize 32、chunks 9、sockets entry/x1/exit0/exit1（door）+ street0/1（2.2 × 3.2）、窓 322（lit 137 / dark 185）、wallConcrete 16.3 m の棟 9、銘板 A-1…、街灯 PointLight 40（0xffe2a0）、path 3 | `L.instances` は空（PropRepetition apartmentBlock は Generator の棟に任せて追加無し）。夜の団地街路を目視、良好 |
| M02 | 502 | r2 未記録室 | ok | 入室中: `mapView.hiddenRoomIds` が Set で現在部屋を含む、`mapCell.hidden===true`、modifierState {policy self, mapErased [r2], entries 1}。退室後（r1）: hidden 維持、`visited===true`、visitedCount 2 で不変。再入室で entries 2。`game.save()` → `?nolock=1` でリロード: 再入室前に `mapCell.hidden===true`（`mapView.hiddenRoomIds` は undefined）、再入室で hiddenRoomIds に復元・entries 2 | ミニマップに M02 の塗りが無いことを目視 |
| M08 | 508 | r1066 不可能なショートカット | warn | 準備: seed 508 で enterRoom 連鎖 39 部屋（depth 29）→ `save()` → `?nolock=1&force=M08` で再読込 → 隣室 3 つに入ると 2 hop 先 r1066 に M08（depth 32）。`FarLink` {resolved true, targetRoomId r1007, distance 5, portalId exit0}、`exit0` が `seam && far && !projected`、先は L09（visited、非 Adapter）、log `INFO far link r1066/exit0 -> r1007`、E → 0.9 s 後 `currentRoomId===r1007` | **距離**: 手元の無向 BFS では r1 が距離 32、距離 ≥30 の既訪部屋が 2 つあるのに distance 5 の L09 が選ばれた。原因は `RoomGraph.bfsDistance` が Portal の向き（`targetRoomId`）だけを辿る有向 BFS で、一方通行 Seam（Legendary 遠方配置 r1011 → L09 の entry は戻り Portal でない）の手前へ戻れず候補が L09 以降に切れるため。仕様の「グラフ距離 30 以上」が Seam を跨ぐと成立しない |
| M14 | 514 | r2 照明一本の虚空 | ok | Bridge L 字、`lights.length===1`（ddd8f4、intensity 1.1、distance 16）、`ambient===0`、lightPanel 1 枚 + 吊り金物 metal、器具と光源の XZ 一致、`render.fog` {000000, 2, 18} = built.fog = scene.fog/背景、hemi 000000 | スクショ: 黒の中に蛍光灯 1 本。意図どおり |
| M19 | 519 | r2 方位破壊区画 | ok | 直線廊下、入室 36 step で 93.1°（90 + drift）、7.5 s で 115.0°、22.5 s で 65.0°、range 65〜115°、`modifierState` に MapRotation 無し。退室: sim 60 step では 65° のまま（戻しは実時間基準）→ 実待機 0.8 s で 0°。再入室で再び 90°+ | ミニマップが約 100° 回った状態を目視（廊下が横倒し） |

集計: ok 18 / warn 5（U17: seed 17 でブース 1 つ / R09: 34 m × 4 m スクリーンで映像が引き伸ばされ暗い / L13: instances 12464 が上限超え + ledBlue 詰め物残存 / M08: 有向 BFS のため Seam 越しの遠方候補が拾えず distance 5 / （軽微）U05 は WorldManager 側で真の出口が施錠）/ fail 0 / notPlaced 0。全 23 部屋で例外・ERROR ログ・不変条件違反なし。コンソール error は全室で基準の 7 件のまま。

## ブラウザ検証 3/4（U02 U06 U10 U14 U18 R02 R06 R10 R15 R19 E03 E08 E13 E17 L01 L05 L10 L14 L19 M03 M11 M16）

手順: 1/4・2/4 と同じ（`?nolock=1&new=1&seed=<S>&force=<ID>` → 開始 → `spawnPointOf` → `teleport` → `enterRoom` → `step(1/60)` × 120。入室前後の `layoutFor(n).boxes.length` / `sockets.length` 不変、`player.pos` が `worldBounds` 内、`world.log` の ERROR 0 件、コンソール error が基準の 7 件（HMR 時の残骸）から増えない、を全室で確認）。Tier: high。

共通の注意:
- 開始オーバーレイのクリックが 1 回だけ効かず `game.state === 'start'` のまま進めていた（R19 の 1 回目）。`state !== 'playing'` の間は `runModifierUpdates` / `BuiltRoom.effects` が回らない（uTime が進まない等）ので、以後は `document.getElementById('start').click()` で開始して `game.state === 'playing'` を確認してから計測した。
- rAF が止まっている間は画面が更新されないので、スクリーンショット前に `renderer.render(scene, camera)` を手で 1 回呼んだ（結果の輝度は同じ render の `readPixels` 平均 = 「luma」）。
- 実時間基準のもの: NoiseGate の帯の下降（holdSec 0.4 を `ctx.now` 実時間で判定。sim step の連続では最高帯が残る）、Game.transition の遷移、E17 のマイク要求。DynamicLength / DynamicMapNode の swap / 秒針 / パーティクルの uTime は sim step で進む。
- テレポート先が家具の中だと天井の上や 2F の橋に押し出される（U10 机、R02 棚、E17 家具、L19 橋）。harness 側の事象。
- サインの `dir` は「面の向き（部屋の内側）」で、ソケットの `dir`（外向き法線）とは逆。サイン正面に立つときは `pos − dirVec × d`。

| ID | seed | 部屋 | status | 確認した事実 | 備考 |
|---|---|---|---|---|---|
| U02 | 2 | r3 重複客室階 | ok | L 字ホテル廊下、`dn:*` 10 枚 全て高さ 1.9、番号 207/203/204/205/206/207/202/209/202/211（重複 2 組）、実扉 side0 の右脇 0.86 m に「202」。end / side1 は removedSockets で実扉 1 枚のみ（実扉同士の重複ペアはこの seed では検証不能） | doc の「脇 0.8 m 以内」式は 0.88 で外れる（板は右 0.85 m にある） |
| U06 | 6 | r3 3:17 の待合室 | ok | LargeRoom 37×30 + 翼、`fs:clock:0..2` 全て '3:17'、`effects===1`、アナログ秒針は Group>Group>Mesh で `rotation.z` が 120 step で −2.125 → −2.334（2π/60 × 2 s = 0.209 ✓、実時間でも進む）、`updateSign('fs:clock:0','3:18')` ok。赤 LED 「3:17」・アナログ時計を目視 | doc の `o.children[0]?.geometry` は 1 段浅い（秒針は孫） |
| U10 | 10 | r2 壁を向く教室 | ok | 22×22 教室、`PropOrientation.clock`（'10:08'、dir 3）→ d=1（東壁）、d の辺にソケット 0、机 143 台 全て 0.5×0.6×0.72 solid（d=1 の向き ✓）、黒板 wallGreen、教卓 1。机列が黒板を向くのを目視 | 22×22 なので机は doc の 50〜70 より多い |
| U14 | 14 | r2 受話器の上がったオフィス | ok | `modifierState.AudioEvent = {portalId 'exit0', done false}`、audio loops 1（preset 受話器の無音・回線ノイズ）。exit0 の開口 0.5 m 内側に入ると `done true`・loops 0。退室 → 再入室でも loops 0（鳴らない） | 未訪問先: exit0/2/3 + hole |
| U18 | 18 → 180 | r2 反復エレベーター | ok | サイン `rd:num` emissive、atlas 文字「6 F」（spec.text '5 F' は layout 時の仮値）、壁扉の先は U18 でない、籠 E → `elevatorCar` Adapter。seed 18: `world.runHooks(籠, end)` は `{forceDefinitionId 'U18', role 'repeat', repeat 1}` を返すが U18 の配置に失敗 → 前室 → 通常抽選（U05）で反復終了（既知の制約）。seed 180: 籠の end → r20 = `U18 / role 'repeat' / repeat 1`、`modifierState {index 1, base 2, headRoomId r2}`、サイン「3 F」（B2F に配置） | hooks.ts の elevatorCar 配線は有効 |
| R02 | 22 | r2 無電源アーケード | warn | RetailGrid 44×44、`lights` 5 本全て 0x9fd0ff / distance 5、`screenGlow` 103、天井の点灯パネル無し（lightOff 81）、`areaEmitters true`、`ambient 0x202428`、hemi 202428 @0.32、背景 0b0d14 | **見た目**: 描画平均輝度 134（比較: 通常点灯の C02 廊下 152）で床・天井が白く明るく「部屋全体が暗い青」に見えない。103 枚の screenGlow（emissive 1.6）の焼き込みが床・天井を持ち上げていると思われる |
| R06 | 26 | r2 青い水族館通路 | ok | 直線 27 m、`render.fog {1a4a8a, 6, 35}` = `built.fog` = scene.fog / 背景、材質名 `\|f1a4a8a:6.0:35`、audio 低周波・水音 [subRumble, waterFlow]。奥が青い霧に沈む | — |
| R10 | 30 | r2 ゲートのない空港 | ok | Terminal 22×67 + 翼、`GATE A/B/C` 3 枚、`fs:board:0/1`「出発 DEPARTURES」、案内板の行先に実隣室（オフィス休憩室 C14 / 市役所待合室 C10 / 病院外来廊下 C05）と無関係な名（照明一本の虚空 / 単一商品スーパー / 3:17の待合室）が混在するのを目視 | 1024 px map の Mesh は 7（ゲート板 3 + 案内板 4 面）で doc の 2 ではない |
| R15 | 35 | r2 絨毯化する設備通路 | ok | U 字廊下、`gradient {from floorConcrete, to floorCarpetRed, axis [1,0,0]}`、`path` 4 点、lightWarm と lightPanel 両方、材質名 `\|gfloorConcrete>floorCarpetRed@1.00,0.00,0.00:-1.5,9.0`、end → U12（部屋型）。入口コンクリ + 冷白（luma 94）、奥は赤絨毯 + 暖色（luma 29）を目視 | — |
| R19 | 39 | r2 無人喫茶店 | ok | 22×22、`particles.type 'steam'`、aabb 2.0×1.2×1.1（底面 2.2 m² < 60）、粒 28（20〜60）、Points 28 ≤ cap、`effects 1`、`uTime` が 60 step で +1.0。カウンター上の湯気を目視 | — |
| E03 | 103 | r2 横向きホテル | **fail** | 直線 2×26 m。`roll 1 / rollAxis 'z' / rollFrom 32 ≥ shellCount 26`、進行扉 end/side0..3 全て `crawl / sill 0.6 / height 1.0 / width ≤ 2.1`（end は 1.5）、entry 2.1、end → crawl Adapter → C09、`built.doors.get('end').crawl && sill 0.6`、ヒント「E: 扉を開ける（しゃがんで通る）」。しゃがみ通過: 開扉 → 0.5〜1.1 m 手前からジャンプ + しゃがみ + 前進で r7（y 0.6）へ通過。Mesh の bbox で照明が側壁（x ≈ −0.83 の縦板）、偽扉が床 0〜天井 2.7 に分布（ロール適用）。**床扉**: `holes[0]` 0.7 × 1.4、hole Portal → r12（entry type hole、配置済み）だが、穴の中心 x = 0.30 / 0.30000001 / 0.29999999 / 0.31 / 0.35 に立って 90 フレーム待っても y = 0 のまま落ちない | `floorDoorHole` の幅 = clamp((廊下幅 − 1.35) × 2, 0.6, 1.0) → 幅 2.0 の廊下では 0.7 m。プレイヤー AABB は幅 0.7（PLAYER.radius 0.35）なので浮動小数の丸めで必ず隣の床に支えられ、穴に落ちられない（真下の部屋 r12 へ行けない）。幅 2.5 廊下（穴 1.0）なら通れる想定。再現: `?nolock=1&new=1&seed=103&force=E03` → `L.holes[0]` の中心へ teleport → `y` が 0 のまま。end スロット幅 1.5 は doc の「1.6 に丸まる」と不一致（軽微） |
| E08 | 108 | r2 / r21 内部拡張会議室 | ok | 殻: 4×4 h2.6、sockets entry/inner、inner は seam（施錠なし）、log `INFO intentional seam r2/inner`、`modifierState.role 'shell'`、銘板 `nev:inner=会議室`。inner を E → r21 `role 'interior' / def E08`、`parentRoomId r2`、entry が seam+far → r2、log `INFO far link r21/entry -> r2`、18×21 m（378 m²）h3.5、exit0〜2、銘板 `nev:interior=会議室`、長机 + 椅子 + ホワイトボードを目視（B1F に配置）。内部の entry を E → 殻の入口内側 (0,0,−1.5) に着地、visitedCount 3 | 銘板は扉の左（doc は右脇） |
| E13 | 113 | r2 遠ざかる廊下 | ok | 直線 2.5×64 m（CorridorGenerator の E13 直線化が有効）、`dynamics` 4（end/door/trim/skirt）、end `solid / min z 8 / axis [0,0,1] / period 1e9 / amp 53.95`、可動域式 ok（endMax 61.95）、RoomBuilder 側 `dynamic/*` 4 が非表示・非ソリッド、`DynamicLength/*` 4 クローン（end ソリッド）、`effects 2`。spawn で `{d 1.5, e 10.25}`、ダッシュ 3600 フレームで毎フレーム `e === min(8 + 1.5d, endMax)`、377 フレーム目で `visible false` かつ end コライダ `solid false`、実扉の前まで到達（ヒント「E: 扉を開ける」）。spawn へ戻すと d 1.5 / e 10.25 / solid true に戻る（位置の純関数） | teleport 直後 1 フレームは偽端が視点を通過して壁が視界を覆う（ゲーム操作では起きない） |
| E17 | 117 | r2 音声認証扉 | ok | GenericRoom 36×36 h5。gate は 2 枚（gate1 は WorldManager が壁へ戻し、gate2 は dead-end 施錠 → 「この扉は開かなそうだ」）、exit*/穴なし、サイン `ng:gate0:dash 🔊 走` / `ng:gate2:still 🔈 静` / `ng:meter`、`bands {gate0 'dash', gate2 'still'}`。gate0（走）: 静止 E → 「この扉は音に反応しない… いまの音は「静」だ」開かず、歩行（level 0.32）E → 「歩」開かず、ダッシュ（0.67）E → 開く。メーター atlas 文字 `▮▮▮▯▯▯▯▯ 歩` が更新。最初の E でマイク許可要求（ペインではブロック → `micAvailable false`、移動代替で動作） | 帯の下降は実時間 holdSec なので、sim step 連続で「静→歩→走」を続けて試すと最高帯が残る（実待機 0.8 s を挟んで検証） |
| L01 | 301 | r2 永久薄明都市 | ok | StreetGrid 64×64 h12（depth 1 直接配置）、`chunkSize 32` / chunks 4、`floorAsphalt`、sockets entry:door / street0,1:street / exit1:door、窓 553、建物（>3 m ソリッド）8、街灯 PointLight 32（`lightColor 0x9fb8ff`、ledBlue 灯具）、サイン 19、`path 3`、`render.fog {2b2f4a, 30, 160}` = built.fog、scene.fog far 90（Tier クランプ）、`fogFar 160`。薄明の街路・建物・車・横断歩道を目視（luma 67） | — |
| L05 | 305 | r2 屋内学園都市 | warn | MegaAtrium campus 149.5×120.5 h9.6、boxes 1036 / shellCount 226、`chunkSize 28` / chunks 30、`fogFar 90`、`path 2`、up0/up1（y 3.6）、theme ゾーン 6 全て `mod 'ZoneThemeShuffle' / pool 'campus'`、preset 6 種（重複なし）、`built.zones` theme 6、water ゾーン（slow 0.55）+ waterShallow、2.2 m 間仕切り 12、lights 59 | **見た目の整合**: Generator のゾーン名と ZoneThemeShuffle の preset が食い違う（図書室→gym / 中庭→gallery / 教室棟→playarea / 食堂→restroom / 体育館→classroom、プール→pool のみ一致）。Generator の内装はそのまま、材質・照明色だけ別テーマになる（体育館の内装に教室の材質、など） |
| L10 | 310 | r2 夜間郊外住宅地 | ok | StreetGrid 64×64 h9、chunks 4、sockets entry/x1/exit0/exit1 + street0〜2、`windowLit` 225 / `windowDark` 0、PointLight 16 本全て 0xffa040、`areaEmitters true`、`ambient 0x2a2c38`、天井灯なし、建物 9。夜の街路と点灯した窓を目視（luma 43） | doc の窓と扉の XZ 重なり式は 4 件引っかかるが全て y 3.7〜5.0 の上階窓で開口（2.1 / 3.2 m）と重ならない |
| L14 | 314 | r2 温室都市 | ok | MegaAtrium greenhouse 166.5×152.5 h10.2、`palette.ceiling 'glass'`、theme ゾーン 15（庭園 / 店舗跡 / 遊歩道）、windowLit あり、`instances` plant 9000（上限）+ grass 13305 = InstancedMesh count 合計 22305、solid 'plant' 1038、boxes 2178、chunks 36、up0/up1。各地上出口の 1.6 m 内にソリッド無し。ガラス天井・吊り灯・植栽の球を目視 | 入口→各出口の直線帯は Generator の棚・生垣で塞がれる（Mega の格子通路で回り込む前提。全出口到達の flood fill は未実施） |
| L19 | 319 | r2 凍結リゾート | warn | MegaAtrium resort（126×93.5 + ホテル翼 44×56）h9.6、boxes 649、chunks 28、theme ゾーン 凍結プール / 雪面（tag 'snow'）/ ホテル翼、`ice` 箔 1（52×81.5、top 0.03、非ソリッド）= friction ゾーン 1（0.2）、snow 箔 5、`particles 'snow'` Points 1000 = cap、`effects 1`、`render.fog {dfe7ee, 10, 80}` = scene.fog、`zonesOf 4`。滑走: 氷上で 1 s 歩いて離すと 2.04 m / 284 フレーム滑る、雪の段丘上では 0.12 m / 12 フレームで止まる | **見た目**: 雪床の描画色が (134,121,107) のベージュ灰で白く見えず、氷面も暗い灰（材質 cfe6f2 / roughness 0.06 だが照明が hemi 8a94a4 @0.32 + 疎な点光で暗い）。明るい霧（dfe7ee）で遠景だけ白い |
| M03 | 503 | r2 移動座標室 | ok | Z 字廊下、進行扉 end/side0/side1/side2 全て seam・非施錠、`INFO intentional seam` ×4、`rewire:*` サイン 4 枚 emissive、atlas 文字 = 行先名、`pending {end U11, side0 C10, side1 C20, side2 C11}` = `localFlags['seam:*'].forceDefinitionId`。床を見たまま step → 1750 フレームで `swapCount 1`、`log [{a side0, b side2}]`、pending が交換され flags 一致、サイン文字も交換（side0=体育館更衣室 / side2=市役所待合室）。交換後 side0 を E → r14 `definitionId === forcedDefinitionId === 'C11'` = pending | — |
| M11 | 511 | r2 多重扉室 | ok | AtriumLobby 44×44 h9、扉 exit0〜4、主扉 exit0 は物理接続 → r7、鏡像 exit1/2/3 が `mirrorOf 'exit0' / seam / far / targetRoomId r7 / 非施錠`、exit4 は通常 → r8、`placedNeighbors` に r7 が 1 つ、`void` 箔 3、`far link r2` ログ 3。主扉を E → 同フレームで鏡像 3 枚 open、`main.open = false` → 次フレームで鏡像 3 枚 close。鏡像のヒント「E: 扉を開ける」、`main.locked = true` → 「この扉は開かなそうだ」（戻して確認）。鏡像 exit1 を E → r7（U05）の入口内側 (0,0,1.5) に着地。閉じた鏡像は木扉パネルとして描かれる（void 箔はパネルと壁の間） | 開閉アニメ中に撮ると戸口が真っ黒に見える（意図どおり） |
| M16 | 516 | r2 没部屋博物館 | ok | Gallery 36×36 h5、`modifierState.GraphReference {mode 'unlisted', count 12, exhibits 12}`（C05 R06 L19 L14 C19 U11 R02 E08 M17 U04 C04 L01）、全て `!rarity`、`discoveredIds` [C02, M16] に含まれない、`gr:*` サイン 12（plate）、`built.signs` 12、`GraphReference/photo*` 12、名札は定義名のみ。額縁 + 暗い写真面 + 名札の列を目視 | rooms.json の params は `mode 'past'` だが GraphReference.ts が `def.id === 'M16'` で 'unlisted' に固定（意図どおり） |

集計: ok 18 / warn 3（R02: 無電源なのに明るい / L05: ゾーン名と preset の不一致 / L19: 雪床・氷面が白く見えない。U18 は seed 18 で配置失敗により反復せず = 既知の制約なので ok 扱い）/ fail 1（E03: 幅 2.0 の廊下では床扉の穴が 0.7 m でプレイヤー幅 0.7 と同じため落ちられない）/ notPlaced 0。全 22 部屋で例外・ERROR ログ・不変条件（入室前後の boxes / sockets）違反なし。コンソール error は全室で基準の 7 件のまま。

## ブラウザ検証 4/4（U03 U07 U11 U15 U19 R03 R07 R11 R16 R20 E04 E09 E14 E18 L02 L07 L11 L15 L20 M04 M12 M17）

手順: 1/4〜3/4 と同じ（`?nolock=1&new=1&seed=<S>&force=<ID>` → `#start` クリック → `spawnPointOf` → `teleport` → `enterRoom` → `step(1/60)` × 120。入室前後の `layoutFor(n).boxes.length` / `sockets.length` 不変、`player.pos` が `worldBounds` 内、`world.log` の ERROR 0 件、コンソール error が基準の 7 件（HMR 時の残骸）から増えない、を全室で確認）。Tier: high。全 22 部屋が depth 1 に直接配置（`legendary far placement` 無し）。

共通の注意:
- ローカル→ワールド変換は `placement.position` + `yawQ`（yawQ 2 は (x,z) → (−x,−z)）。yaw 0 はワールド −z を見る。
- Browser ペインのスクリーンショットは、JS ツールと同じブロックで発行すると初期ロード時の古いフレーム（開始オーバーレイ）が返ることがある。`renderer.render` → `requestAnimationFrame` 待ち → 単独でスクリーンショット、で安定した。
- 実時間基準のもの（AudioEvent の発火、FakeExit のヒント 3 s / 2.5 s）は `await setTimeout` の実待機 + `step` で確認。移動・ベルト・可動壁は sim step。
- 高さ 1.2 m の crawl 扉（R16）は、立った視点（1.6 m）の水平レイが扉の上を通り HUD ヒントが空になる。`player.pitch` を下げる（または しゃがむ）とヒント「E: 扉を開ける（しゃがんで通る）」が出る。

| ID | seed | 部屋 | status | 確認した事実 | 備考 |
|---|---|---|---|---|---|
| U03 | 3 | r2 無人エスカレーター | ok | LargeRoom 44×44。force ゾーン 1 `{conveyor, speed 0.8, lane 0}` vector [0,0,1]、aabb 幅 1.0 × z 1.25〜42.75、`rubber` 箔 1（top 0.015）、`ledBlue` あり、`0x79b9cf` PointLight 3、`belt/0` Mesh、`effects 1`、`map.offset.y` が 1 フレーム 0.0333 ずつ流れる、ベルト上に内装ソリッド 0。乗ると 0.8 m/s（60 フレームで 0.80 m）、逆走 2.20 m/s、audio preset 駆動音 [driveHum] | 「停止した暗いベルト 1 本」は rubber では無い（rubber 箔は 1 枚のみ。金属 27 個は確認）。スクショ: 青縁の暗いベルトが奥へ |
| U07 | 7 | r3 室内雨漏り広場 | ok | Atrium 44×44 h8.67。`particles.type 'rain'`、aabb 6×6 m（部屋より小）、`render.wetness undefined`、水溜まり `water` 箔 1（top 0.012）、`particles/rain` Points 504 ≤ cap 1000、`effects 1`、`uTime` 2.0（120 step）、decals 15 | 雨粒の白い縦筋を目視（柱の影で一部隠れる） |
| U11 | 11 | r2 手荷物受取所 | ok | LargeRoom 36×36。`instances` = [upholstery 81, metal 81]、`signs` 1「BAGGAGE CLAIM」（入口上 y 2.55）、`rubber` 非ソリッド 21 + `metal` ソリッド 21（コンベアのリング区間）、内装材質は columnConcrete / light* / metal / rubber のみ（元家具は消えている）、built.signs 1 | 低い黒いコンベアリングとスーツケースを目視 |
| U15 | 15 | r3 単一商品スーパー | ok | RetailGrid 44×44 h3。`instances` 1 = upholstery 5136（0.28×0.36×0.24）、furnitureLight 詰め物 0、shelfMetal 74、InstancedMesh 4 個（2136/1064/1236/700 = 5136） | 全棚が同色（赤茶）の商品で埋まるのを目視 |
| U19 | 19 | r3 無人キッズスペース | ok | LargeRoom 36×36。`rides undefined && zones undefined`、preset 遠い笑い声 [laughter]。実時間 39 s で `audio.events` に children 2 件（t 14.4 / 34.6 s）、位置は壁から 0.60 m・高さ 1.2 | 音のみ（見た目は通常の広間） |
| R03 | 33 | r2 無限宴会場 | ok | LargeRoom 36×36 + FogDepth。卓（furnitureLight 1.6×1.6×0.75 solid）42、椅子 instances [upholstery 168, furnitureDark 168] = 4 倍、非ソリッドの wallWhite（クロス）42、`render.fog {e8e4d0?, 15, 60}` = built.fog = scene.fog | 卓 42 は doc の 20〜40 をわずかに超える（36×36 なので妥当） |
| R07 | 77 | r3 地下温室 | ok | LargeRoom 36×36。`instances` = [plant 711, grass 856]、solid 'plant' 箱 117、各扉前 1.8 m にソリッド 0。0.25 m 格子の flood fill で entry → exit0 / exit3 全て到達可能（生垣は通路を塞がない） | 葉の塊（球）と生垣の列を目視。luma 48 でやや暗い |
| R11 | 111 | r2 年代混在ホテル | ok | 折れ廊下 3 矩形。theme ゾーン 5 全て `mod 'EraPreset'`、era 1970s→1990s→1990s→2010s→1970s（`Z[0]` は 1970s）、`trim` 帯 2（= 5 − 3）、光色 2 種、built.zones theme 5。入口で preset「空調・古いチャイム・低いハム・遠いBGM」、2 番目の区画（1990s）へ入ると「…蛍光灯・空調」に切替 | 8〜10 m の 2 m 区画あり（既知の短区画）。赤絨毯 + ベージュ壁 → 白の区画を目視 |
| R16 | 116 | r2 縮尺異常オフィス | ok | LargeRoom 36×36 h5。crawl ソケット 1 = `exit0 {0.7 × 1.2, crawl true}`、先 r8 は `adapter.kind 'crawl'`、`built.doors.get(portalId).crawl === true`、巨大偽扉（doorWood solid 高 4.5）1、`footprint` / `height` は通常。通過: 立ったまま E → ヒント空・開かない（レイが扉上を通る）。pitch −0.6 + しゃがみ（eye 0.75）→「E: 扉を開ける（しゃがんで通る）」→ E で open → しゃがみ前進で r8（crawl Adapter）へ通過 | 机の大小混在を目視。偽扉高 4.5（doc の 天井−0.15 = 4.85 と微差） |
| R20 | 200 | r2 巨大トランクルーム | warn | StorageGrid 44×44。doorMetal 非ソリッド 187（2.2×2.4×0.05 が 168 + 0.8×0.5×2.44 の詰め物 19）、`instances` = [metal 168, wallDark 672]、`dn:*` サイン 48（上限）全て `/^[A-Z]-\d{3}$/`、重複 2 組、高さ 1.95、全 48 枚の pos から dir 逆へ 0.05 進んだ点が doorMetal 板の中（板の手前 0.012） | **見た目**: 番号板（0.5×0.125 m、plate）が 2.5 m の距離で暗い板 + 白い点（天井灯の反射）に見え、文字がほぼ判読できない。左隣の板はかろうじて "A-1xx" が見える |
| E04 | 104 | r3 地下の昼光室 | warn | LargeRoom 36×36。`skyNoon` 箔あり、`lights 1`、`ambient 0xb8c4d8`、directional dir [0,−0.5,−0.866]（仰角 30.0°）color 0xffe2b8、方位 = exit0（dir 0）の外向き ✓、`sky.preset 'noon'`、梁（columnConcrete 非ソリッド top = height）13、パネル灯 0、`effects 1` | **見た目**: U20 / R04 と同じく `skyNoon` が diffuser の格子模様として明るく光り、青空には見えない（luma 168 で白っぽい）。梁と暖色の壁面は確認 |
| E09 | 109 | r3 垂直水面オフィス | ok | LargeRoom 36×36。`waterWalls` 1 = exit3（`door 2.2 × 3.0`, dir 3）、Portal `open true / locked false / seam false / passedAt undefined`、`waterWall/exit3` Mesh（material 'waterWall'）、扉パネル非表示・interactables に無し、`effects 1`、waterShallow 箔あり、他の出口 exit1 は 1×2.1。水壁の前で「壁の一枚が水になっている」+ preset 水圧低音 [waterPressure]、E で何も起きず、歩いて抜けると r11 へ + 「水の壁を抜けた」、抜けた後も `open true` | 水面越しに隣の廊下が揺らいで見えるのを目視 |
| E14 | 114 | r2 予測写真室 | ok | Gallery 36×36。`modifierState.GraphReference {adjacent, 4, exhibits[U20, C04, C08, null]}` = `portals`（exit1→U20 / exit3→C04 / exit4→C08、4 枚目は行き先無し = 黒）、`gr:*` サイン 4、built.signs 4、`GraphReference/photo*` 4（atlas 1024×192、明画素 34%）、名札「屋内モーテル中庭」「モール従業員通路」を目視、写真は footprint シルエット（U20 矩形 + レア度ドット、C08 の多矩形） | 写真面は黒地に暗灰のシルエットで薄暗い |
| E18 | 118 | r3 動く壁紙区画 | ok | CorridorHotel 幅 2.0 の 3 矩形（h 2.7）。`dynamics` 3 全て `MovingWalls:*` solid、厚 0.08、幅 1.86〜2.45、高 2.68、slide 振幅 0.62〜0.87、`period === max(2, 2·amp/0.2)`（6.2〜8.7 s）、呼吸なし（幅 2.0）。RoomBuilder 側 `dynamic/MovingWalls:*` 3 は非表示・非ソリッド、`MovingWalls/MovingWalls:*` 3 が可視・ソリッド、120 step で 0.2〜0.4 m 移動（0.2 m/s）、`effects 2`。停止規則: パネル 1 の進路 1.0 m 先に立つと 0.317 m 進んで停止（120 step）、プレイヤーは押されず（移動 0、y 0）、離れると再び動く | 壁紙色のパネルが偽扉の手前を滑るのを目視。最初の計測で `mesh.position`（グループローカル）をワールドと誤用し部屋外へ teleport した（harness 側） |
| L02 | 302 | r2 室内海洋 | warn | MegaHall sea 153×102.5 h11.5、boxes 493 / shellCount 99、chunks 24、`fogFar 90`、theme ゾーン 桟橋区/外海/水門区、water ゾーン 8（slow 0.7）= waterShallow 8、ride ゾーン 1、`GATE n` サイン、出口幅 2.2 ×2、桟橋（floorWood solid）25・上面 0.6、`render.wetness 0.3`。VehicleRide: `rides[0]` boat / 25 s / path 1 = `modifierState.VehicleRide.socketId 'exit1'`、Portal `ride && seam && !locked && !open`、扉前「E: 乗る」→ `state 'riding'`、`VehicleRide/streaks` visible + 8.7 m 流れる、5.5 s 後「E: 到着を早める」→ E → r22 `adapter.kind 'platform'`、`P.far true`、log `ride start` / `ride r2/exit1 -> r22`。桟橋上を 30 m 歩いて y 0.6 維持（減速なし） | **見た目**: 各扉（entry / exit0 / exit1）の正面 2.7〜3.3 m に `columnConcrete`（0.6 m 角、シェル側）が扉軸上に立ち、桟橋（幅 2.4）の中央を塞ぐ。spawn（1.5 m）から前進すると 0.85 m で柱に当たる（左右 0.9 m ずつで回り込める）。到着後も `streaks.visible === true`（doc は乗車中のみ） |
| L07 | 307 | r2 垂直オフィス世界 | warn | MegaAtrium office 52×96.5 h23.4（variant 3、6 階）、boxes 1245 / shellCount 693、chunks 8、`fogFar 60`、`elevators 2` + elevator Portal 2、`up*` 全て dir 2（≠3 ✓）、ガラス間仕切り solid 42、`mapCell.levelSpan 6`、up1 (y 18) / up2 (y 10.8) の先が同じ y に配置、階段最上段 3.6k（k=1..5）あり、`path 2` | theme ゾーンが 2（西オフィス / アトリウム）で共通要件の ≥3 に満たない。中央は 2F スラブ下で暗い（luma 48〜56） |
| L11 | 311 | r2 環状モノレール都市 | ok | MegaAtrium ring 158×158 h9.6、`footprint 4`（帯幅 17.5）、`path 6` / `path[0][1] 4.2`、`windowDark` あり、出口 4 本 幅 2.2（外周）、全 Portal の先が配置済み、`portals.some(ride)`、`seam && !ride && !isReturn` 0（LoopTopology は辺を作らない）、`far` 無し、theme ゾーン 南駅/北駅/西駅/東駅、`ST. S/N/W/E` サイン 4、y 3.6 のホーム（幅 2.35）に立てる、chunks 36、monorail 30 s（exit1）前で「E: 乗る」 | 中庭側を向くと暗いガラス壁で真っ黒に見える（仕様）。帯の内側は青白い照明の回廊 |
| L15 | 315 | r2 屋内高速道路 | ok | RoadGraph: footprint 4（本線 12×70 + 分岐 3）h6.8、chunkSize 32 / chunks 6、floorAsphalt、窓 0、`lane` ゾーン 4（vector 長 >0.9）= `force` ゾーン 4（conveyor 6 / 6 / 3 / 3）、ソケット entry + ramp1 (y 3.6) / rampN0 / ramp0（全て 'ramp'）、`EXIT 1 ←` 等のサイン、holes 0、`belt/0` `belt/1` あり、`effects 1`。左車線に乗ると 1 s で 6.0 m（zoneForce 6）。立体ランプ: 段 0.15 刻み 24 段で 3.6、踊り場に立てる、ramp1 の先 r7 = `adapter 'ramp'` が y 3.6 に配置 | doc の `RAMP ↑ 3.6 m` サインは無い（signs: EXIT 1〜3 / THROUGH TRAFFIC ↑ / 非常口）。暗いトンネル + 路肩の車を目視 |
| L20 | 320 | r2 永久万博会場 | ok | StreetGrid 64×64 h11（variant 0）、shellCount 112（>50。床・天井がブロック分割）、chunkSize 32 / chunks 4、floorAsphalt、sockets entry/x1/exit1（door）+ street0/1（2.2×3.2）、窓 224、建物 27、lights 31、signs 28（`PAVILION n` 10）、`path 3`、theme ゾーン 9 全て `mod 'ZoneThemeShuffle' / pool 'any'`、preset 9 種重複なし、built.zones theme 9 | Generator のゾーンをそのまま使うため間仕切り 0・プール preset でも water ゾーン無し（既知の方針）。パビリオンと発光サインを目視 |
| M04 | 504 | r3 偽帰還口 | ok | SmallRoom 12×12。`modifierState.FakeExit.portalId 'exit3'`、Portal `seam false / locked false` → r10 = `definitionId 'C02'`（= r1）/ `role 'fakeStart'`（Adapter 無し）。exit3 を E で開けて r10 へ入ると HUD「— 終 —」（0〜2.97 s）→「発見 3 部屋 — 探索は続く」（〜5.44 s）→ 空、`shown true` | — |
| M12 | 512 | r2 巨大 Common | ok | 廊下 12×54 + 42×12（L 字）、`height 10.8`、全ソケット door 1×2.1、`holes 0`、chunks 4、`lights[0].intensity 16`（×4²）、パネル灯 3.6×2.4 / 4.8×2.4、入口ラベル通常サイズ（y 2.35） | 巨大な廊下の奥に通常寸法の扉を目視 |
| M17 | 517 | r2 旧バージョン階 | warn | 廊下 L 字。`render.style 'legacy'`、`lights` 2 本全て 0xffffff、`decals / particles undefined`、group の材質 5 種全て `…|slegacy`、扉は `doorWood|slegacy`、テクスチャ 6 枚全て NearestFilter（32 px。ラベルは 128）、labels 1 | **見た目**: 段階陰影 + ドット感は出ているが、wallCream の壁が暗い青紫に見え、全体が暗い（luma 37）。旧版らしさはあるが「クリーム色の壁」には見えない |

集計: ok 17 / warn 5（R20: 番号板が暗く判読しづらい / E04: 空箔が diffuser 格子で青空に見えない（U20・R04 と同根）/ L02: 全扉の正面 2.7 m に杭柱が立ち桟橋中央を塞ぐ / L07: theme ゾーン 2 で共通要件 ≥3 未満 / M17: 壁が暗い青紫）/ fail 0 / notPlaced 0。全 22 部屋で例外・ERROR ログ・不変条件（入室前後の boxes / sockets）違反なし。コンソール error は全室で基準の 7 件のまま。
