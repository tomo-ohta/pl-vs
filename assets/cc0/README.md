# assets/cc0

CC0 1.0 の素材置き場（Poly Haven / ambientCG）。取得は `python3 tools/fetch-cc0-assets.py`（再実行で再開）。
一覧と出典は `manifest.json`、選定の経緯は `../../docs/cc0-assets-plan.md`。
ゲームが読むのはここではなく `tools/build-cc0.mjs` が `public/cc0/` へ書き出したもの。
